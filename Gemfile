source "https://rubygems.org"

ruby "3.2"

# Use the GitHub Pages gem to ensure the CI environment builds the site with the
# same Jekyll/plugins versions that Pages uses. This also provides jekyll and
# common plugins as dependencies.
gem "github-pages", group: :jekyll_plugins
